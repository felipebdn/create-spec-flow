# Shared contracts by area

Files here are contracts reused by more than one change, but they are not mandatory context. Read
only contracts cited by the change or materially related to the domain being changed.

When `shared/conventions.md` grows beyond approximately 400 lines, extract a coherent area into this
directory and leave only the index and routing rule in conventions.
